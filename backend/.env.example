# Database
DATABASE_URL=postgresql+asyncpg://litevault:litevault_dev@localhost:5432/litevault

# Environment
ENV=development

# Authentication
# Options: clerk, mixed, dev
# - clerk: Require Clerk JWT for all protected routes
# - mixed: Accept Clerk JWT or X-Dev-User-Id fallback
# - dev: Accept only X-Dev-User-Id (no Clerk verification)
AUTH_MODE=mixed

# Clerk Configuration (required if AUTH_MODE is clerk or mixed)
# Get these from your Clerk dashboard
CLERK_JWT_ISSUER=https://your-instance.clerk.accounts.dev
CLERK_JWKS_URL=https://your-instance.clerk.accounts.dev/.well-known/jwks.json
# CLERK_AUDIENCE= (optional, for audience validation)

# Enrichment Worker
ENRICHMENT_POLL_INTERVAL_SECS=2
ENRICHMENT_MAX_RETRIES=3

# LLM Settings
# Provider: stub (dev/test, no API calls) or litellm (real LLM)
LLM_PROVIDER=stub
# LiteLLM model identifier examples:
#   OpenAI:   openai/gpt-4o-mini
#   Gemini:   gemini/gemini-1.5-flash (recommended for Gemini)
#   Anthropic: anthropic/claude-3-haiku-20240307
LLM_MODEL=gemini/gemini-1.5-flash
# Comma-separated fallback models (optional)
LLM_FALLBACK_MODELS=
# LLM parameters
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=1024
LLM_TIMEOUT_SECONDS=30
LLM_MAX_RETRIES=2
LLM_CONCURRENCY=3

# LLM API Keys (set the one matching your model)
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=your-gemini-key
# ANTHROPIC_API_KEY=sk-ant-...

# Logging
LOG_LEVEL=INFO

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000

