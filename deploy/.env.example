# Domain (used by containerized Caddy for TLS)
DOMAIN=your-domain.com

# Port bindings (host-caddy mode only)
BACKEND_PORT=8000
FRONTEND_PORT=3000
# Database
POSTGRES_USER=litevault
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD
POSTGRES_DB=litevault

# Clerk Authentication
CLERK_JWT_ISSUER=https://your-instance.clerk.accounts.dev
CLERK_JWKS_URL=https://your-instance.clerk.accounts.dev/.well-known/jwks.json
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...

# CORS - must match your frontend domain
CORS_ORIGINS=https://your-domain.com

# LLM Provider
LLM_PROVIDER=litellm
LLM_MODEL=openai/gpt-4o-mini
OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=
# ANTHROPIC_API_KEY=

# Frontend API URL
# Leave empty for production (same-origin via reverse proxy)
# Set to http://localhost:8000 for local dev without proxy
NEXT_PUBLIC_API_BASE_URL=

# Logging
LOG_LEVEL=WARNING
